{
  "name": "frontend-dev",
  "dockerComposeFile": ["./docker-compose.yml"],
  "service": "app",
  "workspaceFolder": "/app",
  "remoteUser": "root",
  "forwardPorts": [8080],
  "postCreateCommand": "yarn cache clean --force && rm -rf /app/node_modules && yarn install --network-timeout 120000",
  "postStartCommand": "yarn dev --host 0.0.0.0 --port 8080",
  "runArgs": ["--memory=9g", "--cpus=4"]
}
